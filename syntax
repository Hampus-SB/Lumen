class string {
	ui8 *arr;
	i32 length;
	bool literal;
}

bool __lu_string_free(string *s) {}

void foo() {}
ptr bar() {}

string a() {
	string x = input();
	return x;
	// free x
}

i32 main() {
	bool b = false;

	i32 *a = 50;
	f32 b = bitcast(a);

	i32 c = 25;
	f32 d = float(c);
	i64 x = 54;
	f64 y = float(x);

	string s1 = "hello";
	string s2 = a();

	return 0;
	// free all strings
}
